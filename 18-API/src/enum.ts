export enum EUserRoles {
    ADMIN = 1,
    USER = 2,
    SHIPPER = 3,
    STOREKEEPER = 4
};

export enum ERowStatus {
    NEW = 1,
    ACTIVE = 2,
    ON_CONSIDERATION = 3,
    DELETED = 4,
    DISABLE = 5 
};

export enum EItemTypes {
    TYPE1 =	1,
    TYPE2 = 2,
    TYPE3 = 3
};
